<Window x:Class="MediSearch.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Update" Height="220" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA"
        Loaded="Window_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Downloading update" FontSize="22" FontWeight="Bold" Foreground="#17202A" />
        <TextBlock x:Name="StatusText" Grid.Row="1" Text="Preparing download..." Margin="0,12,0,0" Foreground="#344054" TextWrapping="Wrap" />
        <ProgressBar x:Name="DownloadProgress" Grid.Row="2" Height="18" Margin="0,20,0,0" Minimum="0" Maximum="100" />
        <Button x:Name="CloseButton" Grid.Row="3" Content="Close" MinWidth="96" Padding="12,8" HorizontalAlignment="Right" IsEnabled="False" Click="CloseButton_Click" />
    </Grid>
</Window>
